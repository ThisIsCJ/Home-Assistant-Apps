# Debian base (not Alpine) to match the repository's other add-ons and for
# straightforward apt installs of git, hugo and the better-sqlite3 toolchain.
build_from:
  amd64: ghcr.io/home-assistant/amd64-base-debian:bookworm
  aarch64: ghcr.io/home-assistant/aarch64-base-debian:bookworm
